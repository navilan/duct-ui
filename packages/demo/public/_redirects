# Cloudflare Pages redirects file

# Exact /docs path should serve docs.html
/docs /docs.html 200

# Any URL starting with /docs/ should serve docs.html
/docs/* /docs.html 200

# All other URLs should serve index.html (landing page)
/* /index.html 200