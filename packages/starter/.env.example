# Search Worker Configuration
# URL for the Cloudflare Worker that provides search functionality
VITE_SEARCH_WORKER_URL=/api

# Authentication token for securing access to the search index
SEARCH_INDEX_AUTH_TOKEN=dev-token-for-local-testing